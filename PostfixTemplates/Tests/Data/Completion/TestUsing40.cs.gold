// ${COMPLETE_ITEM:using}

public class CoolType : System.IDisposable
{
  public CoolType GetSomething()
  {
    using (var coolType = GetSomething()) {caret}
    using (var coolType = GetSomething()) { }
  }
}